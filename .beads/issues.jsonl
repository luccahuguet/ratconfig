{"id":"ratconfig-554","title":"Add display labels for nested scalar fields","description":"## Why\n\nHosts can model nested settings such as `ui.pane_frames.rounded_corners` as ordinary scalar fields, but Ratconfig currently renders the field path as the visible row label and details title. That is fine for shallow config paths and noisy for nested leaves in source-backed tabs. A small generic display-label/group hook lets hosts present nested scalar leaves cleanly without teaching Ratconfig KDL, product-specific sections, contracts, or migrations.\n\n## Evidence\n\n- `src/model.rs:81`: `ConfigUiField` has `path`, `tab`, `kind`, values, description, validation, apply status, and edit behavior, but no separate display label or group\n- `src/model.rs:98`: `ConfigUiFieldRowSpec` likewise has only `path` and `tab` as display/routing metadata\n- `src/render.rs:339`: setting rows render `field.path` in the setting column\n- `src/render.rs:409`: field details title also renders `field.path`\n- `../yazelix-zellij/zellij-utils/src/kdl/mod.rs:5099`: Zellij has nested UI frame settings under `ui { pane_frames { rounded_corners ... } }`, which are scalar leaves even though their storage shape is nested KDL\n\n## Scope\n\nIn scope:\n- Add optional host-supplied display metadata for fields, likely a compact label and/or group/section string\n- Keep `ConfigUiField.path` as the stable identity used for search, edit intents, host validation, and writes\n- Render nested scalar fields more cleanly in the row list and details pane when display metadata is present\n- Include `path` somewhere in details so agents/users can still inspect the exact storage identity\n- Add focused tests for default behavior and custom label/group behavior\n- Update README examples only where needed to explain the generic display-vs-identity split\n\nOut of scope:\n- Do not add a KDL parser, KDL writer, or KDL schema model to Ratconfig\n- Do not add Yazelix, Zellij, Mars, rounded-corners, keybinding, or runtime-refresh policy to Ratconfig\n- Do not add config contracts, versioning, migrations, or source-file creation policy\n- Do not change JSONC/TOML patch or contract semantics\n\n## Implementation Notes\n\n1. Choose the smallest model extension that keeps existing single-file/simple-field construction ergonomic\n2. Prefer additive optional fields over replacing `path` as the primary field identity\n3. Search should continue matching the real `path`; matching display label/group as additional text is fine if it stays cheap and deterministic\n4. Ensure narrow terminal rendering remains stable and truncates labels/groups predictably\n5. Keep tests focused on rendering/model behavior, not on any downstream host storage format\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test` should exit 0 with new rendering/model assertions\n- `cargo test --features crossterm-runner` should exit 0 because the optional runner constructs test models too\n\n## Stop Conditions\n\n- Stop if the implementation starts moving host storage, KDL, product policy, or migration behavior into Ratconfig\n- Stop if display metadata begins replacing `path` in edit intents or write routing\n- Stop if the UI change makes existing simple fields noisier or harder to scan\n\n## Maintenance Notes\n\nDependency decision: add no crates. This is model/rendering polish over existing strings. Rejected alternative: teach Ratconfig complex KDL for `rounded_corners`; the actual edit is still a boolean leaf, so generic field display support is the smaller and more reusable primitive.","status":"closed","priority":2,"issue_type":"task","assignee":"lucca","created_at":"2026-06-27T21:18:11.830535696Z","created_by":"lucca","updated_at":"2026-06-27T22:18:14.147414764Z","closed_at":"2026-06-27T22:18:14.147267386Z","close_reason":"Completed: added ConfigUiField::display_label and ConfigUiFieldRowSpec::display_label as host-supplied presentation metadata while keeping path as the stable edit/write identity. Rows and field detail titles render display_label when present, details include the exact path when the label differs, search still matches the real path and also matches display labels, and edit intents continue to emit path. Updated README with the display-vs-identity split. Added focused render/model/editor coverage. Dependency decision unchanged: no new crates. Verified with cargo fmt --all -- --check, cargo test, cargo clippy -- -D warnings, cargo test --features crossterm-runner, cargo clippy --features crossterm-runner -- -D warnings, cargo test --no-default-features, and cargo clippy --no-default-features -- -D warnings.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["api","improve","multi-config","ui"]}
{"id":"ratconfig-5h9","title":"Drop JSONC adapter after Yazelix TOML migration","description":"## Why\n\nOnce Yazelix no longer uses `settings.jsonc` as its active user config, ratconfig's JSONC adapter becomes legacy scope rather than core product value. The current docs explicitly justify JSONC because it is the current Yazelix config format. Keeping both JSONC and TOML duplicates storage-adapter and contract surfaces, keeps `jsonc-parser` in the dependency graph, and makes ratconfig look like a multi-format patching library instead of a focused TOML-backed config editor plus deterministic contract machinery.\n\n## Evidence\n\n- `README.md:3`: Ratconfig currently presents itself as supporting \"JSONC- and TOML-backed settings\"\n- `README.md:18`: ratconfig currently owns \"comment-preserving JSONC and TOML set/unset patch primitives\"\n- `README.md:272`: JSONC is documented as first because it is the current Yazelix config format; TOML is documented as the more common Rust and CLI configuration format\n- `Cargo.toml:11`: `jsonc-parser` is a direct dependency only needed for the JSONC adapter\n- `src/jsonc.rs:30-97`: JSONC set/unset/parse/path APIs are public adapter surface\n- `src/migration.rs:65-105`: JSONC-specific migration/default helpers share a module with format-neutral `MigrationOp` and `MigrationMutation` types used by the TOML adapter\n- `src/contract/jsonc.rs:17-90`: JSONC contract apply/read/write/join/reconcile APIs duplicate the TOML contract surface\n- Current size reference: JSONC adapter surfaces are roughly `src/jsonc.rs` 250 lines, `src/migration.rs` 326 lines, and `src/contract/jsonc.rs` 143 lines, though part of `migration.rs` is shared and should survive\n\n## Scope\n\nIn scope:\n- Start only after main Yazelix has completed and published its TOML active-config migration, and the pinned Yazelix ratconfig consumer no longer needs JSONC for normal operation\n- Remove ratconfig's JSONC-backed public adapter surface: `src/jsonc.rs`, `src/contract/jsonc.rs`, JSONC exports, JSONC examples, and JSONC-specific tests/docs\n- Remove the `jsonc-parser` dependency if no remaining supported surface requires it\n- Preserve format-neutral semantic pieces still needed by TOML, including `MigrationOp`, `MigrationMutation`, `ValueTransform`, `ConfigContract`, `ContractChange`, and `ManualMigrationStep`\n- Split or rename `src/migration.rs` as needed so shared migration operation types are not coupled to a deleted JSONC implementation\n- Update README examples and storage-format prose to present TOML as ratconfig's supported text adapter\n- Replace test fixture paths/examples like `settings.jsonc` with TOML-oriented fixtures where they are only generic ratconfig examples\n\nOut of scope:\n- Do not implement Yazelix user-file migration from `settings.jsonc` to `settings.toml` in ratconfig; main Yazelix owns file IO, adoption, validation, and write policy\n- Do not remove `serde_json::Value` merely because JSONC is gone; it remains a useful format-neutral value representation unless a separate proof says otherwise\n- Do not change TOML migration semantics, contract state semantics, manual-blocker behavior, or host ownership boundaries\n- Do not keep a JSONC compatibility shim or optional JSONC feature unless a supported external consumer or explicit migration window is proven before implementation\n\n## Implementation Notes\n\n1. Verify the precondition first in main Yazelix: active user config is TOML, JSONC is not the normal runtime settings source, and main's pinned ratconfig dependency can consume a JSONC-free ratconfig commit\n2. Search ratconfig and known downstream consumers for JSONC symbols before deleting public API; if the surface is already published and supported externally, require an explicit breaking-change decision\n3. Separate shared migration definitions from JSONC-specific migration execution before deleting JSONC helpers\n4. Remove JSONC modules, exports, error variants, examples, and tests in one coherent cleanup so no dead docs or stale public names remain\n5. Update docs in present-tense current-state language: TOML is the storage adapter; hosts still own schema, validation, file IO, and apply policy\n6. Coordinate the child/main release transaction if main Yazelix must update a pinned git dependency after this deletion\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test` should exit 0\n- `cargo clippy -- -D warnings` should exit 0\n- `cargo test --no-default-features` should exit 0\n- `cargo clippy --no-default-features -- -D warnings` should exit 0\n- `cargo test --features crossterm-runner` should exit 0\n- `cargo clippy --features crossterm-runner -- -D warnings` should exit 0\n- `git diff --check` should exit 0\n- `rg -n \"jsonc|JSONC|jsonc-parser|settings\\\\.jsonc\" src README.md Cargo.toml` should show no live ratconfig JSONC adapter surface; any deliberate historical/migration mention must be justified in the close reason\n- `rg -n \"jsonc-parser\" Cargo.toml Cargo.lock` should show no direct dependency after the removal\n- In main Yazelix, after consuming the ratconfig commit without local overrides, focused config UI/config adapter tests should pass\n\n## Stop Conditions\n\n- Stop if main Yazelix still actively reads or writes `settings.jsonc` through ratconfig for normal runtime behavior\n- Stop if a supported external ratconfig consumer still needs the JSONC public API and the maintainer has not approved a breaking removal\n- Stop if TOML cannot represent an active Yazelix config shape without losing supported behavior\n- Stop if the cleanup starts moving Yazelix-owned user migration, schema validation, file IO, or apply policy into ratconfig\n- Stop if deleting JSONC requires weakening TOML contract/migration tests\n\n## Maintenance Notes\n\nDependency decision before implementation: add no new crates. This should delete `jsonc-parser` and keep `toml`, `toml_edit`, and `serde_json` only where still needed. Rejected default alternative: keeping JSONC as an optional feature after Yazelix leaves JSONC, because that preserves adapter complexity and semver surface without proven user value. Reconsider only if an external consumer or explicit migration support window is documented.","status":"deferred","priority":3,"issue_type":"task","created_at":"2026-06-27T20:03:28.261392900Z","created_by":"lucca","updated_at":"2026-06-27T20:03:28.261392900Z","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["api","cleanup","deferred","improve","jsonc","toml"]}
{"id":"ratconfig-88m","title":"Add text-level default completion helper","description":"## Why\n\nHosts that use Ratconfig contracts often need one more text-level operation after join/reconcile: ensure required default paths exist when the config file is new or partially hand-edited. Today that pushes hosts into repeated parse/get/set loops even though Ratconfig already owns comment-preserving JSONC/TOML patch primitives and `MigrationOp::AddDefault` semantics.\n\n## Evidence\n\n- `README.md:18-20`: Ratconfig owns comment-preserving JSONC/TOML patch primitives, migration operations, and deterministic config contracts.\n- `README.md:22-31`: Hosts still own loading, validation, file IO, atomic writes, apply policy, and state-path choice.\n- `src/toml_adapter.rs:121-154`: TOML migrations already implement `MigrationOp::AddDefault` by parsing, checking path absence, and patching text.\n- `src/contract/toml.rs:69-87`: TOML join/reconcile returns patched text but does not provide a small helper for required defaults outside a versioned change.\n- Sibling adopter evidence: `../yazelix-next/crates/yzn-config/src/main.rs:147-198` currently ensures a file exists, joins/reconciles the contract, then manually parses/checks/sets `open.log_level` when missing.\n\n## Scope\n\nIn scope:\n- Add a small text-level helper that applies required defaults to raw config text while preserving comments and format-specific behavior.\n- Prefer reusing existing `MigrationOp::AddDefault` machinery rather than creating a second defaulting semantic path.\n- Support TOML first; add JSONC too if the shared API is clearly symmetrical and small.\n- Return an outcome that exposes the new text and whether anything changed.\n- Document the helper as a post-load/pre-write text transformation, not as a file owner.\n\nOut of scope:\n- Do not add file IO, config path resolution, atomic writes, or host runtime apply behavior to Ratconfig.\n- Do not add schema validation or product-specific validation.\n- Do not force hosts to use this helper when versioned contract changes already express defaults cleanly.\n- Do not change existing contract semantics or migration ordering.\n\n## Implementation Notes\n\n1. First check whether an existing public `apply_*_migrations_text` plus `MigrationOp::AddDefault` is already sufficient; if so, the right implementation may be a tiny documented wrapper or README example rather than new core logic.\n2. If adding an API, consider shapes such as `apply_toml_defaults_text(raw, defaults)` and maybe `apply_jsonc_defaults_text(raw, defaults)`, where each default is path plus `serde_json::Value`.\n3. Use existing adapter patchers and error types so format-specific limits stay adapter errors.\n4. Add tests for unchanged text when defaults already exist, insertion when missing, and format-preserving behavior around comments/order.\n5. Update README host-boundary prose to emphasize that hosts validate and write the returned text atomically.\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0.\n- `cargo test` should exit 0 and include TOML default completion coverage.\n- If a JSONC helper is added, include JSONC default completion coverage too.\n\n## Stop Conditions\n\n- Stop if the helper starts deciding which defaults exist for a host schema.\n- Stop if file IO or atomic write policy enters Ratconfig.\n- Stop if the helper duplicates `MigrationOp::AddDefault` instead of reusing or clearly wrapping it.\n- Stop if contract join/reconcile behavior changes for existing callers.\n\n## Maintenance Notes\n\nThis bead is about removing host boilerplate while preserving the boundary in `README.md:22-31`: Ratconfig can transform text; hosts decide meaning, validation, storage paths, and when to write.","notes":"Dependency decision before implementation: add no new production or dev crates. Build in-house by wrapping existing ratconfig migration/patch machinery, especially MigrationOp::AddDefault and existing JSONC/TOML text adapters. Rejected alternatives: adding a defaulting framework crate, duplicating format-specific parser/patch logic, or moving host schema/default decisions into ratconfig. Packaging impact: no dependency or runtime closure growth; helper remains pure text transformation and host-owned file IO/validation/apply policy stays outside ratconfig.","status":"closed","priority":3,"issue_type":"task","assignee":"lucca","created_at":"2026-06-27T17:50:17.285541131Z","created_by":"lucca","updated_at":"2026-06-27T18:36:18.566491428Z","closed_at":"2026-06-27T18:36:18.566450110Z","close_reason":"Completed: added JSONC apply_defaults_text and TOML apply_toml_defaults_text helpers that wrap existing MigrationOp::AddDefault migration semantics, return existing migration outcomes, preserve host-owned validation/file IO/apply policy, document usage, and cover unchanged/missing/comment-preserving behavior for both formats. Verified with cargo fmt --all -- --check, git diff --check, cargo test defaults_text, cargo test, cargo test --no-default-features, cargo clippy -- -D warnings, cargo test --features crossterm-runner, and cargo clippy --features crossterm-runner -- -D warnings.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["config","host-integration","improve","toml"]}
{"id":"ratconfig-aew","title":"Reduce duplicated migration operation flow","description":"## Why\n\nJSONC and TOML migrations intentionally share semantics, but the operation loop and safe rename/transform flow are implemented twice. Some duplication is necessary for format-specific parse/patch/error types, but the current shape is worth a focused simplification pass because future migration operations must be kept in sync across both adapters.\n\n## Evidence\n\n- `src/migration.rs:65-98`: JSONC `apply_migrations_text` loops over `MigrationOp` and records shared `MigrationMutation` variants\n- `src/toml_adapter.rs:121-154`: TOML `apply_toml_migrations_text` repeats the same operation ordering and mutation-recording structure with format-specific patch functions\n- `src/migration.rs:118-148`: JSONC rename checks overlap, reads source, refuses destination, sets destination, unsets source, and records one rename mutation\n- `src/toml_adapter.rs:263-293`: TOML rename repeats the same safe rename sequence with TOML parse/get/set/unset/error types\n- `src/migration.rs:151-170` and `src/toml_adapter.rs:296-320`: transform flow is duplicated apart from parser/getter/setter/error constructors\n\n## Scope\n\nIn scope:\n- Look for a smaller shared internal migration driver or helper functions that preserve the existing public JSONC and TOML APIs\n- Keep `MigrationOutcome` and `TomlMigrationOutcome` as distinct public types unless a deliberate public API change is separately approved\n- Preserve operation order, mutation order, skipped-missing behavior, destination collision errors, overlap errors, transform errors, and adapter-specific patch errors\n- Keep format-specific TOML limitations such as null rejection and table-parent requirements in the TOML adapter\n\nOut of scope:\n- Do not change contract join/reconcile semantics\n- Do not change public error enum names or variants\n- Do not add a broad trait/object abstraction if it increases cognitive load or LOC\n- Do not move host schema/default decisions into Ratconfig\n- Do not merge JSONC and TOML patchers\n\n## Implementation Notes\n\n1. Characterize the current behavior with existing tests before editing, especially `migrations_rename_delete_add_default_and_transform_jsonc`, `toml_migrations_rename_delete_add_default_and_transform`, and the rename/overlap tests\n2. Try the smallest helper shape first, likely private functions or closures for parse/get/set/unset/error construction rather than a public trait\n3. Preserve public return types and exact error mapping\n4. Compare LOC and readability after the first draft; revert if the generic driver is longer or harder to audit than the duplicated code\n5. Leave a short maintenance note in the bead close reason explaining the final seam, especially if the candidate is rejected after proof work\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test migrations_rename_delete_add_default_and_transform_jsonc` should exit 0\n- `cargo test toml_migrations_rename_delete_add_default_and_transform` should exit 0\n- `cargo test` should exit 0\n- `cargo test --no-default-features` should exit 0\n- `cargo clippy -- -D warnings` should exit 0\n- `cargo clippy --no-default-features -- -D warnings` should exit 0\n- `git diff --check` should exit 0\n\n## Stop Conditions\n\n- Stop if preserving distinct public error and outcome types makes the shared helper more complex than the current duplication\n- Stop if mutation ordering or error variants drift\n- Stop if the implementation hides format-specific TOML limits behind generic names\n- Stop if tests need to be weakened to make the refactor pass\n\n## Maintenance Notes\n\nDependency decision before implementation: add no production or dev crates. Build in-house with private helper functions or closures only. Rejected alternative from prior simplification pass: aliasing `TomlMigrationOutcome` to `MigrationOutcome`, because it changes the public nominal type. Packaging impact: no dependency or runtime closure growth.","notes":"Starting constrained simplification proof after runner API trim. Dependency decision remains unchanged: no new crates; only private helper extraction is allowed. I will reject/close rather than force an abstraction if preserving distinct JSONC/TOML errors and outcomes makes the code larger or less auditable.","status":"closed","priority":3,"issue_type":"task","assignee":"lucca","created_at":"2026-06-27T18:54:31.370873497Z","created_by":"lucca","updated_at":"2026-06-27T19:30:41.042399240Z","closed_at":"2026-06-27T19:30:41.042363736Z","close_reason":"Completed as rejected after proof attempt: implemented a private generic migration driver using MigrationTextOps that preserved distinct JSONC/TOML public error and outcome types and passed cargo test migrations_rename_delete_add_default_and_transform_jsonc plus cargo test toml_migrations_rename_delete_add_default_and_transform, but the diff grew the code and introduced an adapter layer rather than reducing complexity. Reverted the attempted migration/toml_adapter changes per stop condition. Current conclusion: keep the duplicated migration loops until a smaller seam appears; do not alias TomlMigrationOutcome to MigrationOutcome or hide TOML-specific behavior behind a generic abstraction.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["greenlit","improve","jsonc","migration","refactor","toml"]}
{"id":"ratconfig-bsi","title":"Add return-to-default action for config fields","description":"Problem:\nRatconfig can show current and default values, but users need an explicit, discoverable way to restore a field to its default without manually typing or cycling until it happens to match.\n\nDesired behavior:\nExpose a return-to-default action for fields that have a default value. The action should create the same host-owned unset/default intent path Ratconfig already models, rather than writing files directly inside Ratconfig.\n\nScope:\n- Ratconfig owns the generic UI command, display state, key/menu affordance, and intent emission.\n- Host applications own what \"default\" means for a source, whether unsetting or writing a default is correct, and all file persistence.\n- The action should be disabled or hidden for fields without a default.\n\nAcceptance criteria:\n- A focused field with a default can trigger a return-to-default intent.\n- Fields without defaults do not expose a misleading reset action.\n- The UI makes default reset discoverable without crowding compact config screens.\n- Existing host-owned validation and persistence boundaries remain unchanged.\n- Add focused checks for default intent emission and no-default behavior.","notes":"Dependency decision before implementation: add no production or dev crates. Build in-house with existing ConfigUiField.default_value, ConfigUiIntent::UnsetField, reducer keys, and Ratatui render helpers. Public intent decision: do not add a new reset/default intent for this bead; return-to-default should reuse the existing host-owned unset/default route so hosts keep deciding whether unsetting, writing a default, validation, file IO, and apply policy are correct. Rejected alternatives: writing default values inside ratconfig, adding storage adapter behavior, or exposing reset for fields without an explicit default value.","status":"closed","priority":2,"issue_type":"feature","assignee":"lucca","created_at":"2026-06-27T23:12:55.615534344Z","created_by":"lucca","updated_at":"2026-06-28T01:11:39.194909931Z","closed_at":"2026-06-28T01:11:39.194868129Z","close_reason":"Completed: added a default-aware reset action for config fields without adding a new persistence API. ConfigUiField now has has_default_value() backed by the model builder's no-default sentinel. Normal-mode u is presented as reset default only for fields with defaults and emits the existing ConfigUiIntent::UnsetField route; fields without defaults hide the footer affordance and pressing u returns None with an informational notice. Hosts still decide whether that unset/default route means deleting text, writing defaults, validation, file IO, reloads, and apply behavior. Updated README wording and added focused model/editor/render coverage for default availability, reset intent emission, no-default behavior, and UI hiding. Verified with cargo fmt --all -- --check, cargo test, cargo clippy -- -D warnings, cargo test --features crossterm-runner, cargo clippy --features crossterm-runner -- -D warnings, cargo test --no-default-features, cargo clippy --no-default-features -- -D warnings, git diff --check, and jq -s empty .beads/issues.jsonl.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["config-ui","defaults"]}
{"id":"ratconfig-cvf","title":"Add source IDs to config UI fields and intents","description":"## Why\n\nHosts can already group rows with `ConfigUiField.tab`, but separate managed files need a first-class source identity so edit callbacks can route writes without relying on tab-name conventions or side tables. This matters for hosts that want one UI with tabs backed by distinct config files while keeping file IO and product policy outside Ratconfig.\n\n## Evidence\n\n- `README.md:22`: hosts own loading defaults and user config\n- `README.md:27`: hosts own file IO and atomic writes\n- `src/model.rs:80`: `ConfigUiField` carries `path` and `tab`, but no source/document identity\n- `src/editor.rs:55`: `ConfigUiIntent` variants return `field_index`, `path`, and sometimes `value`, but no source/document identity\n- `README.md:108`: the runner example routes `SetField` by `path` alone\n\n## Scope\n\nIn scope:\n- Add a project-agnostic field source/document identity to the config UI model, likely as a `source_id` on `ConfigUiField` and `ConfigUiFieldRowSpec`\n- Carry that source identity through `BeginEdit`, `SetField`, and `UnsetField` intents\n- Keep `field_index` and `path` behavior intact so simple single-file hosts remain straightforward\n- Add focused editor tests proving quick edit, explicit edit, and unset intents preserve the selected field source\n- Update public exports and README snippets affected by the API change\n\nOut of scope:\n- Do not add host file IO, atomic write helpers, product-specific validation, or runtime apply policy to Ratconfig\n- Do not add Yazelix, Mars, Zellij, Yazi, Home Manager, or command-name policy to Ratconfig\n- Do not add a KDL adapter in this bead\n- Do not change JSONC/TOML patch or contract semantics\n\n## Implementation Notes\n\n1. Choose the smallest source identity shape that supports multiple host-owned documents; a string-like id is enough unless the implementation proves a richer type is needed\n2. Preserve the current single-file ergonomics with a clear default source for existing/simple models\n3. Copy the selected field source into every edit intent emitted by `ConfigUiApp`\n4. Keep validation/parsing based on field kind/path exactly as it is today\n5. Update tests that pattern-match intents so they assert the source id where relevant\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test` should exit 0, including new intent-routing assertions\n- `cargo test --features crossterm-runner` should exit 0 because the optional runner also relays edit intents\n\n## Stop Conditions\n\n- Stop if the implementation requires Ratconfig to read, write, create, or migrate host config files\n- Stop if source identity starts encoding product-specific policy instead of a neutral host-supplied id\n- Stop if preserving existing single-file host ergonomics requires a much larger API redesign\n\n## Maintenance Notes\n\nDependency decision: add no crates. This is model/editor API plumbing and should use existing std/String data only. Rejected alternative: let hosts infer the file from `tab`, because tab labels are presentation/grouping and do not safely identify storage when a host later renames tabs, splits one tab across sources, or has repeated field paths across files.","notes":"User clarification: simple source-backed tabs are non-contract surfaces by default. This bead should not introduce versioning, migrations, joined contract state, or required contract APIs for multi-source tabs. The host should be able to render/edit fields for separate files directly; contracts remain an optional separate host choice only when a config surface actually needs migration semantics.","status":"closed","priority":1,"issue_type":"task","assignee":"lucca","created_at":"2026-06-27T20:46:35.405315386Z","created_by":"lucca","updated_at":"2026-06-27T21:25:18.488198465Z","closed_at":"2026-06-27T21:25:18.488131804Z","close_reason":"Completed: added host-supplied ConfigUiField.source_id and ConfigUiFieldRowSpec.source_id, exported DEFAULT_CONFIG_SOURCE_ID for simple single-file models, and carried source_id through BeginEdit, SetField, and UnsetField intents while preserving field_index/path behavior. Added editor coverage for quick edit, explicit edit save, and unset intent routing across distinct source ids; updated model/crossterm/render fixtures and README snippets. Verified with cargo fmt --all -- --check, cargo test editor::tests, cargo test, cargo clippy -- -D warnings, cargo test --features crossterm-runner, cargo clippy --features crossterm-runner -- -D warnings, cargo test --no-default-features, cargo clippy --no-default-features -- -D warnings, and git diff --check.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["api","improve","multi-config","ui"]}
{"id":"ratconfig-den","title":"Add file action rows for host-owned config files","description":"## Why\n\nHost applications sometimes need a config UI row that opens or creates a native config file instead of editing a scalar value inside Ratconfig. Ratconfig should provide the reusable UI/model primitive for that workflow without taking ownership of host file IO, schemas, or editor policy.\n\n## User-Visible Behavior\n\nA Ratconfig host can show a row for an external/native config file. The row displays a label, path/status metadata, and an action affordance. Activating the row emits a host-handled intent, so the host can create the file if needed and open it in its chosen editor.\n\n## Scope\n\nIn scope:\n- Add a project-agnostic row/action primitive for file-opening config entries.\n- Represent at least label, description, path display text, exists/read-only/error status, and whether create-if-missing is offered.\n- Emit a host-handled intent such as `OpenFile`/`ActivateAction` with stable source/path/action identity.\n- Keep keyboard behavior consistent with existing Ratconfig row activation/edit flows.\n- Provide rendering states for missing, existing, read-only, and error/disabled rows.\n- Add focused model/input/render tests for the new row kind and emitted intent.\n- Document the host responsibility boundary in code/docs where the API is exposed.\n\nOut of scope:\n- Do not create, read, write, or launch editors for host files inside Ratconfig.\n- Do not parse Nushell, Starship, TOML, JSONC, KDL, or any host-native config format in this primitive.\n- Do not add Yazelix-specific paths, text, commands, or policy to Ratconfig.\n- Do not make this a general command runner; keep it scoped to config-file action rows.\n\n## Implementation Notes\n\n1. Host applications own file IO, default contents, editor command selection, apply policy, and product-specific labels.\n2. Prefer extending the existing field/source model if that stays simple; add a separate action-row type only if scalar-field semantics become awkward.\n3. The intent should be deterministic and easy for hosts to match without parsing display text.\n4. Missing-file creation should be represented as state and intent metadata, not performed by Ratconfig.\n\n## Verification\n\n- `cargo fmt --all -- --check` passes.\n- `cargo test` passes.\n- Tests prove activation emits the expected host intent for existing and missing-file rows.\n- Tests prove disabled/error rows render and do not emit a misleading scalar edit intent.\n\n## Stop Conditions\n\n- Stop if the design starts pulling host-specific file IO or editor launching into Ratconfig.\n- Stop if the row type duplicates scalar-field machinery instead of sharing the existing model where practical.\n- Stop if it becomes a generic command palette or arbitrary action runner.","status":"closed","priority":2,"issue_type":"task","assignee":"lucca","created_at":"2026-06-28T12:08:27.111068731Z","created_by":"lucca","updated_at":"2026-06-28T12:18:06.320628Z","closed_at":"2026-06-28T12:18:06.320560790Z","close_reason":"Completed: added host-owned file action rows via ConfigUiFileAction, UiRowRef::FileAction, and ConfigUiIntent::OpenFile. Rows carry stable source/action identity, display path, existing/read-only/missing/error state, create-if-missing metadata, and disabled reasons while Ratconfig still performs no file IO, creation, editor launching, parsing, or host policy. Enter/Space/e activate enabled file rows and disabled rows emit no scalar edit/open intent; focused model/editor/render tests cover visibility/search, existing and missing open intents, disabled behavior, render states, and detail metadata. README documents the host responsibility boundary. Verified with cargo fmt --all -- --check, cargo test file_action, cargo test, cargo clippy -- -D warnings, cargo test --features crossterm-runner, cargo clippy --features crossterm-runner -- -D warnings, cargo test --no-default-features, cargo clippy --no-default-features -- -D warnings, git diff --check, and jq -s empty .beads/issues.jsonl.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["file","host-api","intent","ui"]}
{"id":"ratconfig-gpj","title":"Analyze staged Space edits with Enter-only persistence","description":"Problem:\nSpace currently toggles/cycles a field and persists immediately. That is fast, but it makes exploratory edits risky because a user cannot preview or stage a value before committing it.\n\nProposal to analyze:\nMake Space change the in-memory/editing value only, and make Enter the explicit save/persist action. Escape/cancel should discard the staged value and leave the backing config untouched.\n\nQuestions:\n- Should this apply to all field kinds or only boolean/choice fields where Space is natural?\n- How should dirty/staged state be represented in ConfigUiModel and draw_config_ui?\n- Does Enter save the focused field only, or all staged changes if the model later supports multi-field staging?\n- What should host applications receive through ConfigUiIntent so they remain the owner of file IO?\n\nAcceptance criteria:\n- Decide whether Ratconfig should support staged field edits with Enter-only persistence.\n- If accepted, define the minimal public model/intent contract without host-specific policy.\n- Preserve host ownership of validation, file IO, and apply policy.\n- Add focused checks for Space not emitting a persistence intent and Enter emitting the save intent for a staged value.\n- Record any rejected alternatives in the bead before implementation.","notes":"Implementation decision: accept staged direct-choice edits without adding a new public intent or host persistence API. Existing ConfigUiEditState is the staging state. Normal Space should enter/stage direct-choice edits, especially booleans, and return ConfigUiIntent::None; Enter while editing continues to emit SetField with source_id/path/value. Rejected alternatives: adding multi-field dirty state now, because there is no accepted multi-save contract; changing file IO/apply policy, because hosts own persistence; making every text field stage on Space, because Space is ordinary text input there.","status":"closed","priority":2,"issue_type":"decision","assignee":"lucca","created_at":"2026-06-27T23:12:45.311381017Z","created_by":"lucca","updated_at":"2026-06-28T00:51:26.978970655Z","closed_at":"2026-06-28T00:51:26.978912190Z","close_reason":"Completed: accepted staged direct-choice behavior for booleans using the existing ConfigUiEditState rather than adding a new public intent or host persistence API. Normal Space and Enter on a bool now stage the toggled value and return ConfigUiIntent::None; Enter while editing emits the existing SetField intent with source_id, path, and value; Esc cancels without touching host-owned persistence. Updated the bool control footer and README wording, and added reducer plus optional crossterm-runner coverage for Space not persisting and Enter saving the staged value. Rejected alternatives are recorded in notes: no multi-field dirty state, no host IO/apply policy changes, and no text-field Space staging because Space is text input there. Verified with cargo fmt --all -- --check, cargo test, cargo clippy -- -D warnings, cargo test --features crossterm-runner, cargo clippy --features crossterm-runner -- -D warnings, cargo test --no-default-features, cargo clippy --no-default-features -- -D warnings, git diff --check, and jq -s empty .beads/issues.jsonl.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["config-ui","keybindings"]}
{"id":"ratconfig-i4i","title":"Decide runner helper public API before release","description":"## Why\n\nThe optional crossterm runner recently added several public helper functions around key/event conversion and reducer dispatch. They are useful for tests and embedding, but because `src/lib.rs` re-exports the entire runner module when the feature is enabled, each helper becomes public API. If those helpers are not intended as a long-term API surface, trimming them before publication would reduce semver burden and code surface.\n\n## Evidence\n\n- `src/crossterm_runner.rs:45-76`: four public helpers expose key conversion and dispatch: `crossterm_key_to_config_ui_key`, `crossterm_event_to_config_ui_key`, `handle_crossterm_key`, and `handle_crossterm_event`\n- `src/crossterm_runner.rs:79-106`: the actual host-facing runner entrypoints are `run_config_ui` and `run_config_ui_with_details`\n- `src/lib.rs:20-21`: `pub use crossterm_runner::*` re-exports all public runner symbols when `crossterm-runner` is enabled\n- `README.md:98-119`: the documented runner example imports and uses only `run_config_ui`, not the lower-level conversion or dispatch helpers\n\n## Scope\n\nIn scope:\n- Decide whether the lower-level runner helpers are intentionally public, `pub(crate)`, private, or should be exposed under a narrower module path\n- If trimming is accepted, update tests to call private helpers from the module test scope or assert behavior through the remaining public entrypoints where practical\n- Keep the documented `run_config_ui` and `run_config_ui_with_details` flow stable unless the maintainer explicitly chooses a breaking API cleanup\n- Update README only if the public runner surface changes\n\nOut of scope:\n- Do not change editor reducer key semantics\n- Do not change terminal initialization/restoration behavior\n- Do not add a compatibility shim for a surface that has not been published\n- Do not move host validation, file writes, or apply policy into Ratconfig\n\n## Implementation Notes\n\n1. First determine whether this crate version has been consumed externally in a way that makes the helper API effectively committed\n2. If not committed, prefer the smallest public API that supports the README example and expected host embedding use\n3. Keep `crossterm_key_to_config_ui_key` public only if direct key conversion is a deliberate integration contract; otherwise make conversion/dispatch helpers private or `pub(crate)`\n4. Preserve tests for key conversion and ignored events even if the helpers become private\n5. If the API is intentionally kept, add a note or docs sentence explaining the intended low-level embedding use so future agents do not rediscover this as accidental slop\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test --features crossterm-runner` should exit 0 and include runner tests\n- `cargo clippy --features crossterm-runner -- -D warnings` should exit 0\n- `cargo test` should exit 0 for the default feature set\n- `git diff --check` should exit 0\n\n## Stop Conditions\n\n- Stop if the helper API has already been published or consumed and no explicit breaking-change approval is available\n- Stop if deleting helpers requires weakening key conversion tests\n- Stop if the cleanup changes reducer behavior, terminal lifecycle behavior, or host callback error propagation\n- Stop if the only possible implementation adds more wrapper code than it removes\n\n## Maintenance Notes\n\nThis is not automatically isomorphic: reducing public API can be behavior-changing for downstream callers. Dependency decision before implementation: add no new crates. Build in-house by changing visibility/export boundaries only. Packaging impact: no dependency or runtime closure growth; potential semver/API impact must be called out explicitly before landing.","notes":"Implementation decision: no new crates. Treat lower-level crossterm conversion/dispatch helpers as accidental public API because README documents only run_config_ui and run_config_ui_with_details, and the runner commits are still local/ahead of origin in this branch. Keep the test coverage by using private helpers from the module test scope. Preserve terminal lifecycle, reducer semantics, and host callback error propagation.","status":"closed","priority":2,"issue_type":"decision","assignee":"lucca","created_at":"2026-06-27T18:54:10.607626382Z","created_by":"lucca","updated_at":"2026-06-27T19:31:46.695551546Z","closed_at":"2026-06-27T19:31:46.695507425Z","close_reason":"Completed: trimmed accidental lower-level crossterm runner helper API before publication by making key/event conversion and event dispatch helpers private, deleting the now-unused handle_crossterm_key wrapper, and keeping the documented run_config_ui/run_config_ui_with_details entrypoints stable. Runner tests still cover key conversion, ignored release/non-key events, and dispatch through the private event helper. Verified with cargo fmt --all -- --check, cargo test --features crossterm-runner, cargo clippy --features crossterm-runner -- -D warnings, cargo test, cargo clippy -- -D warnings, cargo test --no-default-features, cargo clippy --no-default-features -- -D warnings, and git diff --check.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["api","crossterm-runner","greenlit","improve","refactor"]}
{"id":"ratconfig-l77","title":"Remove Yazelix schema extension hard-code from ratconfig","description":"## Why\n\nRatconfig is now a project-agnostic child crate, but `src/model.rs` still hard-codes Yazelix's JSON Schema extension key `x-yazelix` when extracting tab order. That keeps a Yazelix-specific schema namespace inside the reusable crate and violates the local boundary that host applications own product-specific schema metadata.\n\n## Evidence\n\n- `AGENTS.md:12`: ratconfig owns project-agnostic Ratatui config editor primitives.\n- `AGENTS.md:14`: Yazelix-specific Home Manager, Zellij, Yazi, runtime-refresh, and command-name policy stay out of this crate.\n- `README.md:5`: README says Ratconfig is project-agnostic and hosts provide their own config schema and behavior.\n- `src/model.rs:317`: `schema_tabs` extracts tabs directly from the schema.\n- `src/model.rs:319`: `schema_tabs` hard-codes `.get(\"x-yazelix\")`.\n- `src/model.rs:642`: the unit test describes the behavior as reusable, but the fixture uses `\"x-yazelix\"`.\n- Main Yazelix currently calls `schema_tabs(&schema, DEFAULT_TABS)` from `rust_core/yazelix_core/src/config_ui/model_builder.rs:18`, so changing this API requires a main integration update.\n\n## Scope\n\nIn scope:\n- Remove hard-coded knowledge of `x-yazelix` from ratconfig.\n- Either make the schema extension key host-supplied, such as `schema_tabs(schema, extension_key, default_tabs)`, or move Yazelix-specific tab extraction fully into main Yazelix.\n- Keep generic fallback behavior and the automatic `advanced` tab policy if it is truly generic; otherwise make that host-supplied too.\n- Update ratconfig tests to use a neutral extension key or host-supplied key.\n- Coordinate the main Yazelix consumer update in the same release transaction if the public API changes.\n\nOut of scope:\n- Do not rename Yazelix's existing `x-yazelix` schema metadata in the main repo unless a main integration bead explicitly decides to.\n- Do not move Yazelix config schema, Home Manager ownership, validation, file IO, or apply policy into ratconfig.\n- Do not do a broad config UI redesign.\n\n## Implementation Notes\n\n1. Decide whether `advanced` is a generic ratconfig convention or a Yazelix UI convention.\n2. If keeping tab extraction in ratconfig, change the API so the host supplies the schema extension key and tests prove a non-Yazelix key works.\n3. If moving extraction to main, delete `schema_tabs` from ratconfig and implement the `x-yazelix` read in main Yazelix's config UI adapter.\n4. Publish the ratconfig child commit before updating main Yazelix's git dependency.\n5. Update main Yazelix to consume the new API or local extraction path and run the coupled release transaction without local overrides.\n\n## Verification\n\n- `cargo fmt --all -- --check` should pass in ratconfig.\n- `cargo test` should pass in ratconfig.\n- A targeted grep in ratconfig, `rg -n \"x-yazelix|Yazelix\" src`, should show no source-owned hard-coded Yazelix schema extension references except deliberately generic non-Yazelix test names.\n- Main Yazelix focused config UI tests should pass after the dependency update.\n- Main Yazelix's Cargo lock should point at the pushed ratconfig commit, not a local override.\n\n## Stop Conditions\n\n- Stop if the main Yazelix consumer cannot be updated in the same cross-repo transaction after a ratconfig API change.\n- Stop if the change starts moving Yazelix-owned schema semantics into ratconfig instead of removing them.\n\n## Maintenance Notes\n\nThis is the only concrete extraction leftover found in the ratconfig source audit. Exact old crate/repo names (`yazelix-ratconfig`, `yazelix_ratconfig`, old GitHub URL) were already absent from ratconfig, yazelix-cursors, and main tracked source surfaces.","status":"closed","priority":2,"issue_type":"task","assignee":"lucca","created_at":"2026-06-17T05:22:39.204857385Z","created_by":"lucca","updated_at":"2026-06-17T11:17:12.430946330Z","closed_at":"2026-06-17T11:17:12.430910346Z","close_reason":"Completed: ratconfig schema_tabs now takes a host-supplied schema extension key instead of hard-coding x-yazelix. Verified with cargo fmt --all -- --check, cargo test, rg -n 'x-yazelix|schema_tabs\\(' src, and a main Yazelix local override smoke test for cargo test -p yazelix_core config_ui.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["boundary","config-ui","ratconfig","yazelix"]}
{"id":"ratconfig-missing-rust-test-lanes-f3w","title":"Fix missing Rust test lane declarations","description":"## Why\n\nShared Yazelix agent rules require every first-party Rust file that contains `#[test]` to declare `// Test lane: ...` with an allowed lane. Several ratconfig files contain tests but lack the lane declaration, which makes the test ownership policy inconsistent and easy to miss during future edits.\n\n## Evidence\n\n- `src/jsonc.rs:210`: contains `mod tests` and `src/jsonc.rs:215` contains `#[test]`, but the file has no `// Test lane: ...` declaration\n- `src/migration.rs:162`: contains `mod tests` and multiple `#[test]` cases, but the file has no `// Test lane: ...` declaration\n- `src/toml_adapter.rs:333`: contains `mod tests` and multiple `#[test]` cases, but the file has no `// Test lane: ...` declaration\n- `src/contract/mod.rs:518`: contains `mod tests` and multiple `#[test]` cases, but the file has no `// Test lane: ...` declaration\n- `src/contract/toml.rs:148`: contains `mod tests` and multiple `#[test]` cases, but the file has no `// Test lane: ...` declaration\n- `src/model.rs`, `src/editor.rs`, and `src/render.rs` already demonstrate the expected `// Test lane: default` file-level pattern\n\n## Scope\n\nIn scope:\n- Add the appropriate `// Test lane: default` declaration to first-party Rust files that contain governed `#[test]` functions and currently lack it\n- Keep the change mechanical unless a file genuinely belongs to a different allowed lane\n\nOut of scope:\n- Do not change test behavior, assertions, fixtures, public APIs, or formatting beyond the lane declarations\n- Do not add new test coverage just to touch these files\n- Do not add or change dependencies\n\n## Implementation Notes\n\n1. Re-run `rg -n \"Test lane|#\\\\[test\\\\]|mod tests\" src` and confirm the current set of files with governed tests.\n2. Add `// Test lane: default` near the top of each missing first-party Rust file, matching the existing style in `src/model.rs`, `src/editor.rs`, and `src/render.rs`.\n3. If a tested file clearly belongs in `maintainer`, `sweep`, or `manual` instead, document that reason in the implementation notes before choosing the non-default lane.\n\n## Verification\n\n- `rg -n \"Test lane|#\\\\[test\\\\]|mod tests\" src` should show every first-party Rust file with `#[test]` has a lane declaration\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test` should exit 0\n\n## Stop Conditions\n\n- Stop if the test lane policy has been changed or removed from the applicable `AGENTS.md` before implementation.\n- Stop if the missing-lane list differs materially from the evidence and the correct lane is unclear.\n- Stop if implementation appears to require behavior changes outside mechanical lane declarations.\n\n## Maintenance Notes\n\n- Dependency decision: no new crates; this is a mechanical policy-compliance cleanup using existing source comments only.\n- Review focus: verify the lane declaration is present in every first-party Rust file with governed tests, not only the five cited examples.","status":"closed","priority":3,"issue_type":"task","assignee":"lucca","created_at":"2026-06-27T17:55:26.256958401Z","created_by":"lucca","updated_at":"2026-06-27T17:57:06.901822522Z","closed_at":"2026-06-27T17:57:06.901779853Z","close_reason":"Completed: added // Test lane: default declarations to every first-party Rust file with governed tests that lacked one. Verified with rg -n 'Test lane|#\\[test\\]|mod tests' src, cargo fmt --all -- --check, and cargo test.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["improve","repo-rules","tests"]}
{"id":"ratconfig-pwt","title":"Render selected config source metadata","description":"## Why\n\nThe current header assumes one active config path for the whole UI. Multi-file hosts need the selected tab/source to show the relevant path, owner, existence, and write mode so users can tell which managed file they are editing. Without this, a UI with separate file-backed tabs can edit the right file internally but still display misleading global metadata.\n\n## Evidence\n\n- `src/model.rs:10`: `ConfigUiModel` has one `active_config_path`, `active_config_exists`, `config_owner`, and `config_read_only` for the whole UI\n- `src/render.rs:75`: `render_header` reads only the model-level owner/write state/path\n- `src/render.rs:82`: missing-file text is derived from the single `active_config_path`\n- `src/render.rs:187`: tabs are rendered from bare `String` labels, with no source metadata\n- `src/model.rs:39`: row filtering is tab-label based, while only the hardcoded `advanced` tab gets non-field operational rows\n\n## Scope\n\nIn scope:\n- Add project-agnostic source/tab metadata that can describe a host-owned config document: id, display label, path, exists/missing state, owner, and read-only/write mode\n- Render header metadata from the selected tab/source when present, with the existing single-model path as a compatibility fallback\n- Keep the `advanced` operational tab behavior intact unless a smaller replacement falls out naturally from the new model\n- Add focused render/model tests for two file-backed tabs with different paths and write modes\n- Update affected README examples after the API shape is settled\n\nOut of scope:\n- Do not load, create, patch, migrate, or atomically write any config file inside Ratconfig\n- Do not include Yazelix-specific path names, Mars/Zellij wording, or Zellij sidecar policy in Ratconfig docs or model types\n- Do not add status rows for product-specific native config surfaces in this bead\n- Do not change text adapter semantics for JSONC or TOML\n\n## Implementation Notes\n\n1. Build on `ratconfig-cvf` so field source identity and UI source metadata share one id vocabulary\n2. Prefer a small neutral type such as `ConfigUiSource`/`ConfigUiTabSource`; avoid a broad document-management abstraction\n3. Keep old single-file construction reasonably ergonomic, either through fallback model fields or a tiny helper\n4. Ensure narrow terminal widths still truncate the selected path cleanly and keep owner/mode/diagnostic metadata readable\n5. Keep tests focused on the contract: selected source metadata, fallback behavior, and no regressions to advanced rows\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test` should exit 0, including model/render tests for selected source metadata\n- `cargo test --features crossterm-runner` should exit 0\n\n## Stop Conditions\n\n- Stop if the implementation starts making Ratconfig responsible for host persistence or config discovery\n- Stop if the API requires product-specific source kinds instead of neutral host-supplied metadata\n- Stop if the header/layout change makes narrow-width rendering less stable than the current global metadata line\n\n## Maintenance Notes\n\nDependency decision: add no crates. This should be model/rendering plumbing over existing `PathBuf`, owner enum, and render helpers. Rejected alternative: keep one global `active_config_path` and ask hosts to put the selected file path in field descriptions; that hides core context in the details pane and makes the header misleading for multi-file editors.","notes":"User clarification: selected-source metadata is UI context only. This bead should not introduce config contracts, versioning, migrations, joined state, or source-file creation policy. Hosts can use it for simple render/edit tabs over separate files; any future contract/migration layer must be an explicit opt-in outside this UI metadata work.","status":"closed","priority":1,"issue_type":"task","assignee":"lucca","created_at":"2026-06-27T20:46:55.281203277Z","created_by":"lucca","updated_at":"2026-06-27T21:48:34.865497143Z","closed_at":"2026-06-27T21:48:34.865420755Z","close_reason":"Completed: added ConfigUiSource metadata and selected_config_source lookup, exported the source API, rendered selected tab source label/path/owner/write mode in the header with legacy active_config_path fallback, kept advanced/unmapped tabs on fallback behavior, documented host-owned source metadata, and added focused model/render tests. Dependency decision unchanged: no new crates; this is UI metadata plumbing over existing PathBuf and owner/read-only fields. Verified with cargo fmt --all -- --check, cargo test, cargo clippy -- -D warnings, cargo test --features crossterm-runner, cargo clippy --features crossterm-runner -- -D warnings, cargo test --no-default-features, cargo clippy --no-default-features -- -D warnings, and git diff --check.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["api","improve","multi-config","ui"],"dependencies":[{"issue_id":"ratconfig-pwt","depends_on_id":"ratconfig-cvf","type":"blocks","created_at":"2026-06-27T20:47:36.759797060Z","created_by":"lucca","metadata":"{}","thread_id":""}]}
{"id":"ratconfig-t2f","title":"Add optional crossterm runner for ConfigUiApp","description":"## Why\n\nRatconfig owns `ConfigUiApp`, `ConfigUiKey`, `ConfigUiIntent`, and `draw_config_ui`, but every crossterm host currently has to write its own raw-mode alternate-screen loop and key mapping before it can use those primitives. This is reusable terminal UI mechanics, not host policy.\n\n## Evidence\n\n- `README.md:13-17`: Ratconfig owns the generic model, edit state, controls, and Ratatui rendering.\n- `README.md:90`: Hosts currently build a model, then use editor/rendering helpers inside their own terminal event loop.\n- `src/editor.rs:41-72`: Ratconfig already defines the generic key and intent vocabulary.\n- `src/render.rs:49-57`: Ratconfig already owns `draw_config_ui` and `draw_config_ui_with_details`.\n- Sibling adopter evidence: `../yazelix-next/crates/yzn-config/src/main.rs:60-132` currently duplicates the crossterm terminal session, draw loop, event read, release filtering, key conversion, and intent dispatch around Ratconfig.\n\n## Scope\n\nIn scope:\n- Add an optional crossterm-backed runner module or helper, gated behind an explicit feature.\n- Keep the runner generic over host save/apply behavior, likely by returning intents to callbacks or accepting small host callbacks for `SetField` and `UnsetField`.\n- Keep `draw_config_ui_with_details` support so hosts with richer details do not lose functionality.\n- Add focused tests for key conversion/dispatch where possible without requiring an interactive TTY.\n- Update `README.md` with a minimal runner example.\n\nOut of scope:\n- Do not move host schema, validation, file IO, atomic writes, config paths, or runtime apply policy into Ratconfig.\n- Do not make crossterm a required dependency for non-TUI/text-patching users.\n- Do not hardcode Yazelix command names, config paths, or notices.\n\n## Implementation Notes\n\n1. Record the Rust dependency decision before coding: crossterm should be optional, behind a feature such as `crossterm-runner`, instead of entering the default text-patching surface by accident.\n2. Extract a small public key-conversion helper first if that removes most host duplication without committing to a large runner API.\n3. If adding a full runner, keep the API callback-oriented: Ratconfig drives terminal lifecycle, rendering, and key/event dispatch; host callbacks perform validation, writes, model reload, and notices.\n4. Preserve existing lower-level APIs so hosts can still own their event loop when needed.\n5. Add tests around non-interactive pieces and keep manual TTY behavior documented rather than pretending CI can prove terminal rendering.\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0.\n- `cargo test` should exit 0 and include coverage for any new key conversion or runner state helper.\n- If a new feature is added, run `cargo test --no-default-features` and `cargo test --features crossterm-runner` or the final feature name.\n\n## Stop Conditions\n\n- Stop if the API starts requiring hosts to surrender validation, file writes, or apply policy.\n- Stop if crossterm must become a default dependency for non-UI consumers.\n- Stop if interactive terminal behavior cannot be separated from testable key/intent logic.\n\n## Maintenance Notes\n\nThis should remain an optional convenience layer. The core Ratconfig contract stays: Ratconfig owns reusable editor mechanics; hosts own config meaning and persistence.","notes":"Dependency decision before implementation: add crossterm only as an optional production dependency behind a non-default crossterm-runner feature. Keep existing ratconfig core and default ui feature free of crossterm. Reuse ratatui's crossterm backend by enabling ratatui/crossterm only for that feature. Build in-house: small key-event conversion and callback-oriented runner glue. Rejected alternatives: making crossterm default, moving host validation/file IO/apply policy into ratconfig, or adding a broader terminal framework. Packaging impact: consumers that do not enable crossterm-runner do not pull the crossterm event-loop surface.","status":"closed","priority":2,"issue_type":"task","assignee":"lucca","created_at":"2026-06-27T17:49:53.471306299Z","created_by":"lucca","updated_at":"2026-06-27T18:19:40.133103676Z","closed_at":"2026-06-27T18:19:40.133068718Z","close_reason":"Completed: added optional crossterm-runner feature with key/event conversion helpers, callback-oriented run_config_ui/run_config_ui_with_details helpers, non-interactive conversion and dispatch tests, and README usage docs. Verified with cargo fmt --all -- --check, git diff --check, cargo test, cargo test --no-default-features, cargo test --features crossterm-runner, cargo clippy -- -D warnings, and cargo clippy --features crossterm-runner -- -D warnings.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["host-integration","improve","ui"]}
{"id":"ratconfig-vwy","title":"Centralize dotted JSON path traversal","description":"## Why\n\nThe current worktree has an isomorphic simplification that removes repeated dotted JSON object traversal from JSONC, model, and TOML adapter code. This should be tracked before committing so the change has the same planning and verification record as the surrounding simplification work.\n\n## Evidence\n\n- `src/jsonc.rs:105`: `get_json_path` is a public wrapper over dotted JSON path traversal\n- `src/model.rs:536`: model-level `get_json_path` exposes the same traversal for config model helpers\n- `src/toml_adapter.rs:117`: `get_toml_path` traverses the JSON value converted from TOML using the same dotted object-key semantics\n- `src/patch.rs:11-38`: the shared patch module already owns dotted path parsing and overlap checks, making it the narrowest existing home for shared dotted-path helpers\n- Current candidate diff: `src/jsonc.rs`, `src/model.rs`, `src/patch.rs`, and `src/toml_adapter.rs` net to about `14 insertions(+), 26 deletions(-)` while preserving public wrapper function names\n\n## Scope\n\nIn scope:\n- Keep existing public paths and names: `jsonc::get_json_path`, crate-root `get_json_path` via `model`, and `toml_adapter::get_toml_path`\n- Centralize only the duplicated traversal mechanics in `src/patch.rs`\n- Keep `dotted_paths_overlap` behavior for invalid paths and strict-prefix checks unchanged\n- Preserve the existing focused default-completion tests and broad adapter tests\n\nOut of scope:\n- Do not rename public helper functions\n- Do not expose a new public helper from `patch` unless a downstream public API need is proven\n- Do not collapse `TomlMigrationOutcome` into `MigrationOutcome`; that changes observable nominal type/debug identity even if field access still compiles\n- Do not change config path syntax, bracket rejection, trimming, or JSON/TOML parse behavior\n\n## Implementation Notes\n\n1. Use the current worktree diff as the candidate implementation, but reread it rather than applying blindly\n2. Keep public wrappers as one-line delegations to an internal shared helper\n3. Keep path overlap semantics equivalent: invalid paths still return `false`, and only strict parent/child prefixes overlap\n4. Run formatting after edits because import ordering changes\n\n## Verification\n\n- `cargo fmt --all -- --check` should exit 0\n- `cargo test defaults_text` should exit 0 and continue to cover JSONC and TOML default completion\n- `cargo test` should exit 0\n- `cargo clippy -- -D warnings` should exit 0\n- `cargo test --no-default-features` should exit 0\n- `cargo clippy --no-default-features -- -D warnings` should exit 0\n- `git diff --check` should exit 0\n\n## Stop Conditions\n\n- Stop if keeping the public wrappers makes the change net larger or harder to read\n- Stop if any path syntax or error behavior changes\n- Stop if a downstream caller would need to import a new symbol\n- Stop if the refactor starts touching contract semantics, schema validation, file IO, or host apply policy\n\n## Maintenance Notes\n\nDependency decision before implementation: add no new production or dev crates. Build in-house with existing `serde_json::Value` and `patch` helpers. Rejected alternatives: public re-export aliases for all callers, because that can change API shape; aliasing `TomlMigrationOutcome` to `MigrationOutcome`, because that changes nominal public type identity. Packaging impact: no dependency or runtime closure growth.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-06-27T18:53:49.677763172Z","created_by":"lucca","updated_at":"2026-06-27T19:15:42.196753858Z","closed_at":"2026-06-27T19:15:42.196718116Z","close_reason":"Completed: centralized duplicated dotted JSON object traversal in patch::get_dotted_json_path while preserving the public jsonc::get_json_path, crate-root get_json_path, and toml_adapter::get_toml_path wrappers. dotted_paths_overlap still rejects invalid paths and only treats strict parent/child prefixes as overlapping. Verified with cargo fmt --all -- --check, git diff --check, cargo test defaults_text, cargo test, cargo clippy -- -D warnings, cargo test --no-default-features, and cargo clippy --no-default-features -- -D warnings.","source_repo":"ratconfig","source_repo_path":"/home/lucca/pjs/yazelix-dir/ratconfig","compaction_level":0,"original_size":0,"labels":["improve","migration","patch","refactor"]}
